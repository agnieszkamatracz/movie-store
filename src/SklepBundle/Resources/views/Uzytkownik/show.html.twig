{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Uzytkownik</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Login</th>
                <td>{{ entity.login }}</td>
            </tr>
            <tr>
                <th>Haslo</th>
                <td>{{ entity.haslo }}</td>
            </tr>
            <tr>
                <th>Ranga</th>
                <td>{{ entity.ranga }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('uzytkownik') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('uzytkownik_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
